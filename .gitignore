# packwiz / build outputs
*.zip
*.mrpack
.packwiz-installer.jar
packwiz-installer-bootstrap.jar

# local test instance (if you point a launcher/server at this repo)
run/
mods/*.jar
config-local/

# Logo/branding working files (deliverables in branding/ are committed;
# gen/ holds source renders + the compose script, kept local)
gen/
bb-*.png

# Playwright MCP browser-automation artifacts
.playwright-mcp/

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/
